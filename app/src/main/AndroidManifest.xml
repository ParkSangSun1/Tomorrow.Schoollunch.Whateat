<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.what.tomorrow_school_lunch">
    <!-- 인터넷 사용권한 -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- 네트워크 상태 액세스 권한 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- Wi-Fi 상태 액세스 권한 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tomorrow_School_lunch">
        <activity android:name=".UI.SchoolPerformMain.SchoolPerformMainActivity"></activity>
        <activity android:name=".UI.newMain.fragment.BottomNavigationActivity"></activity>
        <activity android:name=".UI.tutorial.SchoolSelectionActivity" />
        <activity android:name=".UI.tutorial.TutorialActivity" />
        <activity android:name=".UI.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>